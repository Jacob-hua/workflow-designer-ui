const state = {
  cascaderProps: {
    emitPath: false,
    checkStrictly: true,
  },
};

const getters = {};

const mutations = {};

const actions = {};

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions,
};
